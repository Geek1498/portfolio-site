// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://Geek1498.github.io',
  base: '/portfolio-site/',
  trailingSlash: 'ignore',
});
